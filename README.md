# ktp-project
To run the application install all the dependencies using `pip install -r requirements.txt` (either globally or in a virtual environment) and then run the application using `flask --app main run`.
